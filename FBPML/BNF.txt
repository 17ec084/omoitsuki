/*
FBPML(Fill-in-the-Brank Problem Markup Language)

<FBPML> ::= ("---" <文>)*
<文> ::= (<新規の穴> | <既知の穴> | <htmlコード>)*
//<文>: 曖昧な時は左を優先
<新規の穴> ::= "<" "[0-9]"+ ">" <htmlコード>? "</" "[0-9]"+ ">"
<既知の穴> ::= "<" "[0-9]" "/>"
<htmlコード> ::= <新規の穴の開始または終了タグを含まず、かつ既知の穴を含まないような任意の文字列>

<新規の穴の開始または終了タグを含まず、かつ既知の穴を含まないような任意の文字列>
::= (
    "[^<]"|
    "</?[^0-9]+"|
    "</?[0-9]+[^ />]"|
    "</?[0-9]+ +[^/>]"|
    "</?[0-9]+ >"
)+
*/


「abcまたはdefが含まれない文字列」
→「"(
([^ad])|([ad][^be])|([ad][be][^cf])
)+"」